<%= form_with(model: song_info, local: true) do |form| %>
  <% if song_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song_info.errors.count, "error") %> prohibited this song_info from being saved:</h2>

      <ul>
        <% song_info.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="field">
    <%= form.label :song %>
    <%= form.collection_select(:song_id, Song.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
